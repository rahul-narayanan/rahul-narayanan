version: '3'
services:
  nestjs-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    depends_on:
      - azure-sql-edge
    volumes:
      - .:/app

  azure-sql-edge:
    image: mcr.microsoft.com/azure-sql-edge
    environment:
      ACCEPT_EULA: "Y"
      SA_PASSWORD: YourStrongPassword
      MSSQL_PID: Developer
    ports:
      - "1433:1433"
    networks:
      - backend

  redis:
    image: redis:latest
    ports:
      - "6379:6379"

networks:
  backend:
    driver: bridge
