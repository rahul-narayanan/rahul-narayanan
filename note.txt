 return this.createQueryBuilder('group')
      .leftJoinAndSelect('group.accounts', 'account')
      .addSelect('(SELECT COUNT(*) FROM group_account ga WHERE ga.groupId = group.id) AS accountCount')
      .orderBy('group.id', 'DESC') // Adjust your ordering as needed
      .limit(50)
      .getMany();
