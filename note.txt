<div class="autocomplete-multi-select">
  <div class="selected-options">
    <div class="selected-option" *ngFor="let option of selectedOptions">
      {{ option }} <span class="remove-option" (click)="onOptionRemoved(option)">X</span>
    </div>
    <input class="search-input" type="text" placeholder="{{ props.placeholder }}" (input)="searchInput$.next($event.target.value)" [(ngModel)]="searchTerm">
  </div>
  <div class="options" *ngIf="options$ | async as options">
    <div class="option" *ngFor="let option of options" (click)="onOptionSelected(option)">
      {{ option }}
    </div>
    <div class="no-options" *ngIf="options.length === 0">
      No options found.
    </div>
  </div>
</div>