<mat-form-field appearance="outline">
  <mat-chip-list #chipList>
    <mat-chip *ngFor="let option of selectedOptions" [removable]="true" (removed)="removeOption(option)">
      {{option}}
      <mat-icon matChipRemove>cancel</mat-icon>
    </mat-chip>
    <input type="text" placeholder="Search options" aria-label="Search options" matInput [formControl]="searchControl" #searchInput [matAutocomplete]="auto" (matChipInputTokenEnd)="addOption($event)">
    <mat-autocomplete #auto="matAutocomplete" (optionSelected)="optionSelected($event)">
      <mat-option *ngFor="let option of filteredOptions | async" [value]="option">
        {{option}}
      </mat-option>
    </mat-autocomplete>
  </mat-chip-list>
</mat-form-field>