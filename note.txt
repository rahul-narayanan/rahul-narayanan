location / {
            if ($request_uri ~ ^/proxy/my-app(/?.*)$) {
                set $proxy_my_app_uri $1;

                if ($proxy_my_app_uri ~ ^/proxy/my-app$) {
                    rewrite ^/proxy/my-app$ /proxy/my-app/ permanent;
                }

                rewrite ^/proxy/my-app(/?.*)$ $proxy_my_app_uri break;
            }

            # Your existing / location configuration...
            # ...
        }
