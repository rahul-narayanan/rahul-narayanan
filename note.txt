{
  "extends": "./tsconfig.json", // Extend your main tsconfig.json
  "include": ["src/**/*.ts"], // Adjust this to match your source code location
  "exclude": ["node_modules", "build", "dist"], // Exclude build output directories
  "compilerOptions": {
    // Add any script-specific options here
  }
}
